<nav class="navbar navbar-dark navbar-expand-md bg-dark">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="col-md-2 col-12">
    <button class="navbar-toggler ms-1 btn-outline-light" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand text-white ms-4" id="logo1" href="{{@site.url}}">
      <img src="{{@site.logo}}" class="rounded navbar-logo" />&nbsp;&nbsp;{{@site.title}}
    </a>
  </div>
  <div class="col-md-10 col-12">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto">
        {{#if @site.navigation}}
              {{navigation}}
          {{/if}}
        ...
      </ul>
    </div>
  </div>
</nav>